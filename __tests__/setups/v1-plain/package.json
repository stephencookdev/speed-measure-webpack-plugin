{
  "name": "v1-plain",
  "version": "0.0.1",
  "description": "Test webpack v1 with SMP",
  "scripts": {
    "full-test": "bun run prepare && bun run test && bun run cleanup",
    "postinstall": "bun ../../../scripts/fix-v1-node-libs-browser.js",
    "prepare": "cp -r ../../common/* .",
    "test": "bun test ./smp.fixture.js",
    "cleanup": "rm -rf dist",
    "audit-fix": "bun audit"
  },
  "license": "MIT",
  "packageManager": "bun@1.3.8",
  "engines": {
    "node": ">=6.0.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "7.0.x",
    "css-loader": "^0.28.10",
    "style-loader": "^0.20.2",
    "webpack": "1.x.x"
  }
}
