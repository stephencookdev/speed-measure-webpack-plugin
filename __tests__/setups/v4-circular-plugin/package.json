{
  "name": "v4-circular-plugin",
  "version": "0.0.1",
  "description": "Test webpack v4 and a plugin with circular references, with SMP",
  "scripts": {
    "full-test": "bun run prepare && bun run test && bun run cleanup",
    "prepare": "cp -r ../../common/* .",
    "test": "bun test ./smp.fixture.js",
    "cleanup": "rm -rf dist",
    "audit-fix": "bun audit"
  },
  "license": "MIT",
  "packageManager": "bun@1.3.8",
  "engines": {
    "node": ">=6.0.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.4",
    "css-loader": "^0.28.11",
    "style-loader": "^0.20.3",
    "webpack": "^4.47.0"
  }
}
