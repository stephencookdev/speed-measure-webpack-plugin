{
  "name": "v5-func-proxy",
  "version": "0.0.1",
  "description": "Test webpack v5 with a function proxy set-up with SMP",
  "scripts": {
    "full-test": "npm run prepare && npm run test && npm run cleanup",
    "prepare": "cp -r ../../common/* .",
    "test": "jest ./smp.test.js",
    "cleanup": "rm -rf dist",
    "audit-fix": "npm audit fix"
  },
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "babel-loader": "^8.1.0",
    "css-loader": "^5.0.2",
    "jest": "^26.6.3",
    "style-loader": "^2.0.0",
    "webpack": "^5.24.0",
    "terser-webpack-plugin": "^5.1.1"
  }
}
